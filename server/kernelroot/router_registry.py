# Router registry — user-created scheduler routers.
# Managed by the Assistant. Do not edit manually.
# Each entry: (module_path, router_attr_name)

ROUTERS: list[tuple[str, str]] = [
    ("schedulers.test_scheduler.router", "router"),
    # [ASSISTANT_ROUTERS]
]
